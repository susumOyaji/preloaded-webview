{
	"name": "preloaded-webview",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler pages deploy public",
		"dev": "wrangler pages dev public",
		"start": "wrangler pages dev public"
	},
	"devDependencies": {
		"wrangler": "^4.51.0"
	}
}